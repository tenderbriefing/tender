# Firestore emulator IDOR tests

Requires Java 21+ for the Firestore emulator.

```bash
npm run test:firestore-emulator
```

Uses project `demo-tenderbriefing` and port `8085` (see `firebase.json` emulators).
